以下是 Phase 1 的核心知识图谱整理：

---

### 1. 文件角色与包含逻辑 (C++ 层面)

| 文件类型 | 核心职责 | 包含原则 |
| --- | --- | --- |
| **`.h` (头文件)** | **声明契约**：告诉外部有哪些函数、类、结构体。 | 必须加 **宏卫语句** (`#ifndef`) 或 `#pragma once`。只包含声明所需的最小库（如 `<vector>`）。 |
| **`.cpp` (源文件)** | **实现逻辑**：具体的代码执行逻辑。 | **首行**包含对应的 `.h`。包含实现所需的重型库（如 `<algorithm>`），不暴露给外部。 |
| **`main.cpp`** | **程序入口**：组装各模块，驱动业务流程。 | 显式包含自己直接用到的所有头文件（如 `<iostream>`, `<vector>`），不靠“蹭”。 |

---

### 2. CMake 核心指令 (构建层面)

这是你管理项目“身体”的四把钥匙：

1. **`include_directories(include)`**
* **作用**：把 `include` 文件夹加入全局搜索路径。
* **效果**：你的代码里可以写 `#include "calc.h"` 而不是相对路径 `#include "../include/calc.h"`。


2. **`add_library(CruncherCore src/calc.cpp)`**
* **作用**：将算法实现打包成一个独立的二进制“零件”（静态库）。
* **意义**：实现了逻辑的**封装**和**复用**。


3. **`add_executable(DataApp src/main.cpp)`**
* **作用**：定义最终要运行的那个程序。


4. **`target_link_libraries(DataApp CruncherCore)`**
* **作用**：红娘指令。把“主程序”和“算法库”黏合在一起。
* **注意**：这里必须用**目标名**（Target Name），拼写错误（如写成 `directories`）会导致编译失败。



---

### 3. 开发流程与编译原理

* **编译 (Compile)**：`.cpp` $\rightarrow$ `.o` (或 `.obj`)。将源代码翻译成机器码碎片。
* **链接 (Link)**：多个 `.o` + 库文件 $\rightarrow$ 可执行文件。把碎片拼接成完整的程序。
* **增量编译**：CMake 聪明到只重新编译你改动过的那个文件，这在大项目中能节省几小时的时间。

---

### 4. 工程化最佳实践

* **接口与实现分离**：让使用者只看菜单（`.h`），不进后厨（`.cpp`）。
* **依赖最小化**：不要在头文件中包含不必要的重型库，减小编译压力。
* **源外构建 (Out-of-Source Build)**：将 `build` 文件夹与源码分开（无论是在项目内还是项目外），保持代码目录的纯净。

---

### 5. 调试总结

* **错误排查**：当看到 `Undefined symbols` 时，通常是忘了链接库（`target_link_libraries`）；当看到 `Redefinition` 时，通常是忘了宏卫语句。
* **工具包 (Kit)**：在 VS Code 中，确保选择了正确的编译器版本（如 Apple Clang++）。

---

### 下一阶段导读：Phase 2

在 Phase 2 中，我们将挑战 **`calculateMedian` (中位数)** 算法。你会接触到：

1. **数据拷贝与引用**：什么时候该传副本，什么时候该传原件。
2. **STL 的深度应用**：利用 `std::sort` 处理数据。
3. **类型转换**：如何优雅地处理 `int` 到 `double` 的精度提升。

**这份总结是否涵盖了你目前所有的理解点？如果有哪个细节觉得还需要再推敲，我们可以停下来深挖；如果 OK 了，我们就在 `calc.h` 里写下中位数的第一行声明！**

好的，我们来为**阶段 2：手造轮子与内存深度掌控**做一个精炼的复盘。

这个阶段你不仅用自定义的 `MyVector` 替换了标准库，更重要的是你完成从“使用者”到“设计者”的思维转变：

### 1. 内存管理的“大三律” (Rule of Three)

你通过亲手实现以下三者，彻底解决了 C++ 最臭名昭著的内存错误（如双重释放、野指针）：

* **析构函数**：确保数据在生命周期结束时正确归还给系统。
* **拷贝构造**：实现了**深拷贝**，确保每个对象拥有独立的数据副本。
* **赋值运算符**：运用 **Copy-and-Swap** 技巧，实现了**异常安全**与**自我赋值安全**，这是工业级的标准写法。

### 2. 接口设计的权衡逻辑

你理解了 C++ 在“极致性能”与“类型安全”之间的取舍：

* **`operator[]` vs `at()**`：前者是追求零开销的“信任模式”，后者是带边界检查的“安全模式”。
* **非成员函数重载**：弄清楚了为什么 `operator<<` 必须写在类外，理解了“左操作数决定调用权”的本质。

### 3. 性能优化思维

你通过参数传递的细节，掌握了如何写出高效的代码：

* **`const T&` 的多面性**：既能在计算平均值时作为**观察者**实现零拷贝，也能在数据加工时通过显式拷贝保护原件。
* **动态扩容策略**：实现了 `reserve` 和 `push_back`，通过**倍增扩容**保证了 $O(1)$ 的摊还时间复杂度。

---

**核心感悟：**
你现在看到的 `MyVector` 不再只是一个装数字的盒子，而是一个精密管理的内存控制器。你已经掌握了 C++ 底层开发的“入场券”。

**准备好进入阶段 3，让这个轮子跑在真实的海量数据上了吗？**

方案 A：功能补完计划（增加“众数”与“导出”）
现在的分析结果只能看，不能存。我们可以让 DataCruncher 具备输出报告的能力。

挑战众数 (Mode)：编写一个函数计算出现频率最高的数字。这需要你在不使用 std::map 的情况下，利用已排序的 MyVector 统计连续重复项。

导出结果：增加一个 saveReport(const std::string& filename) 函数，将计算出的平均值、中位数、标准差写入到一个新的 .txt 或 .csv 文件中。

方案 B：性能与健壮性提升（模板化）（已完成）
目前的 DataCruncher 只能处理 int。但现实世界的数据往往是 double 或者 float。

类模板化：尝试将 DataCruncher 改造为模板类 DataCruncher<T>。

思考：当你把 int 换成 double 时，你的 processData（去重）逻辑是否依然准确？（提示：浮点数比较通常不能直接用 ==）。

方案 C：自动化测试（工程化思维）(当前在main函数添加冒烟测试)
随着代码量增加，每次改动 MyVector 都要手动运行一遍 main 来检查是否报错，这太低效了。

编写单元测试 (Unit Test)：在 main.cpp 中建立一个测试函数，自动验证：

读取空文件是否崩溃？

文件全是非法字符时，MyVector 是否为空？

给出一组已知数据（如 1, 2, 3），计算结果是否等于预期？


**阶段三**其实是让你的代码从“学生作业”迈向“软件工程”的关键跨越。

我们可以把这三个核心动作的深层意义再梳理一下：

### 1. 类封装：`DataCruncher` 类的诞生

* **动作**：将散落在 `main.cpp` 中的逻辑（排序、去重、计算）全部收纳进 `DataCruncher`。
* **意义**：实现了**逻辑与展示的分离**。`main` 函数现在只负责调度，而不必关心数据是怎么算出来的。这不仅让代码整洁，更意味着你的 `DataCruncher` 变成了一个**可复用的组件**，可以随时被扔进另一个项目里直接使用。

### 2. I/O 升级：从硬编码到文件读取

* **动作**：实现了支持**容错逻辑**（跳过非数字字符）的文件读取函数。
* **意义**：打破了程序的“孤岛状态”。程序不再只能处理你在代码里写死的几个数字，而是具备了**处理现实世界真实数据**的能力。你还顺便攻克了 C++ 路径管理和文件流状态复位（`clear()`）这两个硬骨头。

### 3. 数学扩展：标准差函数的引入

* **动作**：利用 `MyVector` 迭代器和 `<cmath>` 库实现了标准差算法。
* **意义**：标志着你的工具包具备了**多维分析能力**。现在的 `DataCruncher` 不仅能告诉你数据“是什么”（平均值），还能告诉你数据“稳不稳”（标准差）。

---

### 阶段三完结勋章 🏅

你现在手里握着的是一套带动态内存管理底层（MyVector）**且**具备容错文件解析能力（DataCruncher）的数据分析小系统。

**接下来，你想进入“阶段四”吗？**
在阶段四中，我们通常有两个进阶方向：

1. **交互升级**：让用户在运行时通过命令行输入文件名，而不是在代码里改路径。
2. **性能监控**：如果读入 100 万个数字，你的 `MyVector` 扩容会变慢吗？我们可以给程序加个“计时器”，看看它的极限在哪里。

**或者，你有什么其他的想法？比如想给 `MyVector` 增加更多功能？**