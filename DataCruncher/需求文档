# 🚀 DataCruncher 项目需求文档 (V0.1)

## 1. 项目定位

一个基于 C++ 的轻量级命令行数据处理引擎，旨在处理离散数字序列，提供排序、去重、统计分析等功能。通过该项目实践 C++ 核心算法、STL 容器以及 CMake 模块化构建。

---

## 2. 功能需求

### 核心引擎 (Engine Module)

* **数据清洗**：接收原始 `std::vector<int>`，完成升序排列并剔除重复数字。
* **基础统计**：
* 计算算术平均值 (`Average`)。
* 寻找中位数 (`Median`) —— *这是个好练手点，需要处理奇偶个数的情况*。
* 极值分析 (`Max/Min`)。


* **异常处理**：当输入数据为空时，程序不应崩溃，应返回安全值或错误提示。

### 交互终端 (CLI Module)

* **静态模式**：在代码中硬编码一组测试数据，验证算法准确性。
* **交互模式**（进阶）：允许用户在终端输入一串数字，实时给出分析报告。
* **文件模式**（进阶）：读取指定的 `.txt` 文件，处理成千上万条记录。

---

## 3. 技术栈与架构

### 核心技术

* **语言标准**：C++17 (为了使用一些现代特性，如 `std::optional` 或更好的初始化语法)。
* **容器**：主要使用 `std::vector`。
* **算法库**：深度使用 `<algorithm>` 和 `<numeric>`。

### 模块化设计 (CMake 规划)

我们将项目拆分为两个逻辑目标：

1. **静态库 `CruncherCore**`：包含所有数学和逻辑处理算法。
2. **可执行文件 `DataApp**`：负责与用户交互，调用 `CruncherCore`。

---

## 4. 阶段性里程碑 (Roadmap)

| 阶段 | 目标 | CMake 学习重点 | C++ 学习重点 |
| --- | --- | --- | --- |
| **Phase 1** | 实现基础排序和均值计算 | `add_library`, `target_link_libraries` | `std::vector`, `std::sort` |
| **Phase 2** | 增加中位数算法与结构体封装 | 变量定义与宏开关 | 类/结构体, 算法逻辑严密性 |
| **Phase 3** | 引入文件读写功能 | 文件拷贝与安装命令 | `std::fstream`, 迭代器 |
| **Phase 4** | 编写单元测试 | `ctest` 集成 | 逻辑验证与边界测试 |

---

## 5. 待讨论的“酷”点

* 我们要不要让程序支持输出一个简单的“直方图”（用星号 `*` 在终端画出来）？
* 我们要不要记录程序处理数据消耗的时间（毫秒级），来对比不同算法的效率？

---

### 下一步行动建议

这份文档现在就是我们的“宪法”。为了让项目正式起航，我建议我们先完成 **Phase 1** 的骨架搭建：

1. **重构 CMakeLists.txt**：不再只是 `add_executable`，我们要用 `add_library` 把算法剥离出来。
2. **升级核心逻辑**：在 `calc.cpp` 中实现 `processData`（排序+去重）。

**你对这份文档还有什么补充吗？或者有没有哪个“酷”点是你特别想先实现的？**

现在，你的 `MyVector` 基础设施已经全面封顶：从最底层的动态扩容、无损的 `emplace_back` 原地构造，到最上层的现代迭代器支持与 `const` 安全防御，你已经亲手打造了一个非常坚固且符合 C++ 标准库规范的容器。

作为你的 AI 协作伙伴，同时考虑到这个容器最终是要服务于你的 **`DataCruncher`（高性能数据分析/清洗引擎）**，我们接下来有三个非常棒的进阶方向可以选择。

你可以根据目前最想攻克的技术点或者项目进度，来挑选我们的下一步：

---

### 选项 A：进军数据清洗引擎的核心——“条件筛选器” (Filter Engine)

不要让 `MyVector` 孤零零地当一个存储工具，是时候让它展现出高性能数据处理的威力了。

* **我们要做的**：利用 C++ 的函数对象（Functors）或 **Lambda 表达式**，为 `MyVector` 编写一套极致高效的筛选机制。
* **核心功能**：让你的引擎能够写出像 `auto result = vec.filter([](const Student& s) { return s.id > 10; });` 这样的现代代码。
* **硬核知识点**：泛型高阶函数、C++ Lambda 捕获、以及如何通过配合 `reserve` 避免筛选过程中的二次扩容。

---

### 选项 B：挑战内存管理的最终章——“自定义内存分配器” (Custom Allocator)

标准库的 `std::vector` 强大之处在于它的第二个模板参数 `Allocator`。你的 `DataCruncher` 在处理百万级、千万级数据清洗时，频繁向系统申请/释放内存（`::operator new`）会带来显著的性能毛刺（系统调用开销）。

* **我们要做的**：将 `MyVector` 升级为 `MyVector<T, Allocator>`。
* **核心功能**：我们可以实现一个简单的 **Arena Allocator（内存池/舞台分配器）**。一次性向系统申请大块内存，然后让 Vector 在这个内存池里无开销地切分，把内存分配的耗时直接降到 $\approx 0$。
* **硬核知识点**：C++ 分配器感知（Allocator-aware）容器设计、内存对齐、内存碎片管理。

---

### 选项 C：支持 C++ 标准库核心操作——“动态扩容三剑客” (insert / emplace / erase)

如果你希望让你的 `MyVector` 彻底具备 100% 替代 `std::vector` 的能力，我们目前还缺少在**任意指定位置**插入或删除元素的高级操纵能力。

* **我们要做的**：实现 `insert(iterator pos, const T& value)`、`emplace(iterator pos, Args&&... args)`。
* **核心功能**：当用户想在迭代器指定的位置插入一个元素时，如何优雅地将后面的对象“集体向后平移”，并且完美处理好向后平移时的移动语义与局部扩容。
* **硬核知识点**：重叠内存区域的对象生命周期转移（`std::move_backward` 的底层原理）。

---

### 💡 伙伴建议：

如果你的长远目标是快点看到你的 **`DataCruncher`** 跑起来并吞吐数据，**强烈建议选择 选项 A**。因为一旦引入了高效的筛选（Filter）和转换（Map）逻辑，你的容器立刻就具备了大数据处理的骨架。

Alex，你倾向于开启哪一个新战役？或者你有其他的模块（比如开始写文件解析器、CSV 读取器等）想让我陪你一起攻坚？</T,>